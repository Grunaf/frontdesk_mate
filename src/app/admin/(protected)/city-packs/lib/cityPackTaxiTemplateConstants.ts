/** Guest arrival backup chips — per-hub numeric estimates in admin. */
export const HUB_TRIP_ESTIMATES_SECTION_TITLE = 'Trip estimates';

export const HUB_TRIP_ESTIMATES_INTRO =
  'Used on guest arrival backup price and duration chips for this hub (not the step-by-step text).';

/** @deprecated Use HUB_TRIP_ESTIMATES_* — kept for any stale imports. */
export const TAXI_METADATA_SECTION_TITLE = HUB_TRIP_ESTIMATES_SECTION_TITLE;
/** @deprecated */
export const TAXI_METADATA_INTRO = HUB_TRIP_ESTIMATES_INTRO;

export const TAXI_COST_TEMPLATE_HINT = '{minKM}, {maxKM}, {minEUR}, {maxEUR}';
