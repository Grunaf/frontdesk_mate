-- City pack route content (routes, warnings, preTripTips in city_packs.content).
-- Seeds are applied via: node scripts/migrate-city-pack-routes.js --apply
-- Guest runtime also falls back to code+i18n seed until DB routes exist.

select 1;
